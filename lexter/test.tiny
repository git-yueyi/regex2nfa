{ Sample program 
  in TINY language - 
  computes factorial
}
:
read x; { input an integer}
if (0 < x ) ||(x > -3)then {don't compute if x <= 0 }
  fact := 1;
  repeat
    fact := fact * x;
    x := x - 1
  until x = 0;
  write fact {ouput factorial of x }
  if if (y ===3;
end
